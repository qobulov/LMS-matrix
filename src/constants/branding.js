/**
 * User-visible product name (sidebar, login, copy).
 * Change this string to rebrand the app in one place.
 * Also set the same name in `index.html` <title> if you change it.
 */
export const APP_NAME = "LMS Matrix";
